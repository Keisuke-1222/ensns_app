<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="#">EnSns</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <%= header_link_item('ホーム', root_path) %>
      <%= header_link_item('掲示板', boards_path) %>
      <% if user_signed_in? %>
        <strong><%= header_link_item(current_user.name, root_path) %></strong>
        <%= header_link_item('プロフィール変更', edit_user_registration_path) %>
        <li class="nav-item"><%= link_to 'ログアウト', destroy_user_session_path, method: :delete, class: "nav-link" %></li>
      <% else %>
        <%= header_link_item('サインアップ', new_user_registration_path) %>
        <%= header_link_item('ログイン', new_user_session_path) %>
      <% end %>
    </ul>
    <form class="form-inline my-2 my-lg-0">
      <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
      <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
    </form>
  </div>
</nav>
